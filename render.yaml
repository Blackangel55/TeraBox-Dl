services:
  - type: web
    name: terabox-dl-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 10000
      - key: COOKIE_JSON
        sync: false   # set manually in the Render dashboard
